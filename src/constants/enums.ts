export enum Operator {
  Addition = "+",
  Subtraction = "-",
  Division = "/",
  Multiplication = "*",
}

export enum Sign {
  Equals = "=",
}

export enum BoardRowStatus {
  Invalid = "invalid",
  Valid = "valid",
  Zero = "zero",
  None = "none",
}

export enum KeyboardKey {
  ArrowUp = "ArrowUp",
  ArrowDown = "ArrowDown",
  Delete = "Delete",
  Backspace = "Backspace",
}
